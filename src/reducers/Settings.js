const initialSettings = {
  insightAPI: 'https://explorer.zensystem.io/insight-api-zen/',
  explorerURL: 'http://explorer.zenmine.pro/insight/'
}

export default function SettingsReducer(state=initialSettings, actions){
  return state
}